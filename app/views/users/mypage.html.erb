
<%= render :partial => "application/header"  %>
<% flash.each do |type,msg| %>
  <div class="alert alert-<%=type %>" role="alert"><%= msg %></div>
<% end %>
<h1>MYページ</h1>
    <%= link_to "登録情報変更", users_edit_path, class: 'btn btn-outline-dark' %>
</div>
<% @users.each do |user| %>
  <table class="table table-striped table-hover">
    <tr><th>ユーザー名</th><th><%= user.name  %></th></tr>
    <tr><td>メールアドレス</td><td><%= user.mail  %></td></tr>
    <tr><td>自分の投稿</td><td><button type="button" class="btn btn-primary" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
    クリックして表示↓
  </button>
    <div class="collapse" id="collapseExample">
      <div class="border p-3">
        <%= link_to "新規作成", new_column_path, class: 'btn btn-outline-dark' %>
    <table class="table table-hover boards__table">
      <thead class="thead-dark">
        <tr>
        <th>投稿者ID</th>
        <th>投稿者名</th>
        <th>タイトル</th>
        <th>本文</th>
        <th>画像ファイル</th>
        <th>カテゴリ</th>
        <th>備考</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
  <tbody>
            <tr>
             <% @columns.each do |column| %>
                <th><%= column.userid  %></th>
                <th><%= column.username  %></th>
                <th><%= column.title.truncate(25) %></th>
                <th><%= column.body.truncate(200) %></th>
                <th><%= image_tag column.image.to_s, :size =>'150x150'%></th>
                <th><%= column.category %></th>
                <th><%= column.state %></th>
                <td><%= link_to '詳細', column, class: 'btn btn-outline-dark' %></td>
              </tr>
            <% end %>
          </tbody>
        </div>
      </div></td>
    </tr>
  </table>
<%end%>
